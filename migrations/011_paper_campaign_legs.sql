CREATE TABLE IF NOT EXISTS paper_campaign_legs (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id TEXT NOT NULL,
  campaign_id UUID NOT NULL REFERENCES paper_campaigns(id) ON DELETE CASCADE,
  strategy_id UUID REFERENCES strategy_configs(id) ON DELETE SET NULL,
  strategy_version INTEGER NOT NULL DEFAULT 1,
  cycle_id UUID,
  leg_id TEXT NOT NULL,
  role TEXT NOT NULL,
  side TEXT NOT NULL,
  option_type TEXT NOT NULL,
  execution_rank INTEGER NOT NULL,
  expiry TEXT NOT NULL,
  strike NUMERIC NOT NULL,
  instrument_key TEXT NOT NULL,
  trading_symbol TEXT,
  lot_size INTEGER NOT NULL,
  lots INTEGER NOT NULL,
  quantity INTEGER NOT NULL,
  entry_price NUMERIC NOT NULL,
  exit_price NUMERIC,
  current_price NUMERIC,
  pnl NUMERIC NOT NULL DEFAULT 0,
  status TEXT NOT NULL DEFAULT 'OPEN',
  entry_at TIMESTAMPTZ NOT NULL DEFAULT now(),
  exit_at TIMESTAMPTZ,
  last_mark_at TIMESTAMPTZ,
  metadata JSONB NOT NULL DEFAULT '{}'::jsonb
)